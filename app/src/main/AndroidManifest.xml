<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vandenbreemen.modernsimmingapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="com.vandenbreemen.modernsimming.READ" />

    <application
        android:name=".ModernSimmingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityGroupSelect"></activity>
        <activity android:name=".ActivityAddGroup" />

        <provider
            android:name=".SimContentProvider"
            android:authorities="com.vandenbreemen.modernsimmingapp.SimContentProvider"
            android:readPermission="com.vandenbreemen.modernsimming.READ"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>